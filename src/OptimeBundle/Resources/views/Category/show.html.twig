{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-sm-12">
        <h2>Categor&iacute;a: {{ category.name }}</h2>
        <table class="table table-hover table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Código</th>
                    <td>{{ category.code }}</td>
                </tr>
                <tr>
                    <th>Nombre</th>
                    <td>{{ category.name }}</td>
                </tr>
                <tr>
                    <th>Descripci&oacute;n</th>
                    <td>{{ category.description }}</td>
                </tr>
                <tr>
                    <th>Activa</th>
                    <td>{% if category.active %}<span class="badge badge-success">Si</span>{% else %}<span class="badge badge-danger">No</span>{% endif %}</td>
                </tr>
            </tbody>
        </table>

        <ul class="nav nav-pills">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_index') }}">Volver al listado</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_edit', { 'id': category.id }) }}">Editar</a>
            </li>
            <li class="nav-item">
                {{ form_start(delete_form, {'attr': {'id': 'category_show'}}) }}
                    <input class="btn btn-danger nav-link delete-btn" type="submit" value="Eliminar">
                {{ form_end(delete_form) }}
            </li>
        </ul>
    </div>
{% endblock %}
